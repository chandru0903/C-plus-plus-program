#include<iostream>
#include<cmath>
using namespace std;
int sprt(int n){
    cout<<"The square root of the given number is : "<<sqrt(n);
}
int main(){
    int n;
    cout<<"Enter the number : ";
    cin>>n;
    sprt(n);
}
