#include<iostream>
#include <cmath>
using namespace std;
void yoid(int r){
    float area=3.14*r*r;
    cout<<"area of the circle is "<<round(area);
}
int main(){
    int r;
    cout<<"Enter the radius of the circle : ";
    cin>>r;
    yoid(r);
}
